# Cotizador de Viajes Internacionales — dependencias
#
# Instalación:
#   pip install -r requirements.txt

Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52

# ------------------------------------------------------------------
# Controladores opcionales para usar otro motor de base de datos en
# vez de SQLite (que ya viene incluido en Python, sin instalar nada).
# Descomenta el que necesites y ajusta DATABASE_URL en el entorno.
# ------------------------------------------------------------------
# PyMySQL==1.1.1          # Para MySQL / MariaDB
# psycopg2-binary==2.9.9  # Para PostgreSQL
